1. Git是一種分散版本控制系統，可以用來記錄程式的sourcecode等變更及追蹤歷史。
它可以在每個用戶的工作目錄創建一個完整的儲存副本且含所有的歷史紀錄。
其中歷史紀錄的查閱或變更都可以在無網路且無法access中央儲存數據中心的狀態下完成。
Git是用於以下情況：1. 文件版控的困擾 2. 團隊合作的困擾
Git的特徵是在伺服器上創建一個數據庫給所有的使用者共同使用。可以管理查閱所有的文件歷史紀錄。它還可以恢復到某特定時間的版本。
且不只可以管理sourcecode，excel或圖片皆適用。在有網路的環境，所有團隊的文件及變更紀錄皆可共享，可以避免多人工作常發生的一些問題。

2. list是一種可以允許重複的值及可以變更的且有順序的的集合。
   tuple是一種可以允許重複的值及有順序但不可變更的集合，通常用於固定的data。
   dictionary是一種不允許重複的值及無順序但可變更且有index的集合，key/value pair。
   set是一種不允許重複的值及無順序的集合，unique特性且無索引。
